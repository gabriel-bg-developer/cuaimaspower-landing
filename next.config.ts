import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuraciones específicas de Next.js pueden ir aquí
  /* config options here */
};

// Exportamos la configuración básica de Next.js
export default nextConfig;
